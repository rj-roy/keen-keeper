import React from 'react';

const FriendCard = ({friend}) => {    
    const {
        id, goal, email
    } = friend;

    return (
        <div>
            dfjs
        </div>
    );
};

export default FriendCard;